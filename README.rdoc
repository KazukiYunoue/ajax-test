== Welcome to ajax-test
Let's ajax.

== Getting Started

1. create table:
       rake db:migrate

2. start server:
       rails s

3. Go to http://localhost:3000/articles and you'll see:
       "Reload" link is heavy but "Reload(Ajax)" link is light.
